mixin horizontal
	display flex
	flex-direction row

mixin vertical
	display flex
	flex-direction column

mixin horizontal-wrap
	display flex
	flex-flow row wrap

mixin vertical-wrap
	display flex
	flex-flow column wrap

mixin default-transition
	transition all transition-speed ease

mixin noise-strong
	background-image url("/images/backgrounds/noise-light.png")